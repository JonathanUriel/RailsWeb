<%= form_for(@relevant) do |f| %>
  <% if @relevant.errors.any? %>
      <div id="error_explanation">
        <h2>No se pudieron guardar los datos. <%= pluralize(@relevant.errors.count, "errore") %>: </h2>

        <ul>
        <% @gallery.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

      <div class="form-group">
        <div class="field">
          <%= f.label :Tipo %><br>
          <%= f.text_field :tipo, class: "form-control", value: "Inicio", readonly: "true" %>
        </div>
      </div>

        <div class="form-group">
    <div class="field ">
      <%= f.label :Imagen %><br>
      <%= f.collection_select(:product_id, Product.order(:name),:id, :name, {prompt: "--Selecciona una opción--" }, {class: "form-control"}) %>
    </div>
          </div>

    <div class="actions">
      <%= f.submit "Guardar", class: "btn btn-success" %>
    </div>

<%end%>
